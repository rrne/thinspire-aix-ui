export type FactoryType = {
        id: number;
        title: string;
        electronic: boolean;
        heats: boolean;
        error: boolean;
        location: [number, number];
        code: string
    }
    
export type BigFactoryType = {
        title: string;
        error: boolean;
        location: [number, number];
        code: string
    }
    
